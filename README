Big Streaming - A streaming-like interface for big
--------------------------------------------------

INSTALLATION
Make sure argloop.py is in your PYTHON_PATH and the commands
are in your PATH.  Then run them.

REQUIREMENTS
-Python Client for Big
-Big server

ENVIRONMENT VARIABLES
BIGHOST = hostname for server
BIGPORT = port for server
BIGWORKSPACE = workspace to use
BIGVARIABLE = variable to write to
Arguements have precedence over environment variables.

For more information, pass a -h parameter to any of the
commands.

contact: pat AT big-computing DOT com
License: Apache 2.0
