#!/usr/bin/env python

import sys
from nws.client import NetWorkSpace

ws = NetWorkSpace(sys.argv[1])

if len(sys.argv) < 2:
   sys.stderr.write('Usage: delete workspace [variable]\n')
   sys.exit(1)
elif len(sys.argv) < 3:
   ws.server.deleteWs(sys.argv[1])
else:
   ws.deleteVar(sys.argv[2])
